# COMUSE backend — example configuration.
# Copy to `.env` and fill in your real values. Never commit `.env`.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
RATE_LIMIT_PER_MINUTE=10